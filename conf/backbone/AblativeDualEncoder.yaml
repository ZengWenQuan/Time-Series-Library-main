# @conf/backbone/AblativeDualEncoder.yaml

# Backbone 的类名
name: AblativeDualEncoder

# 初始特征提取器的配置 (内联)
initial_extractor:
  name: MultiScaleInception
  in_channels: 1
  out_channels: 64
  kernel_sizes: [3, 5, 7, 11]

# 分支的配置 (通过 defaults 列表加载)
branches:
  - ${global_branch}
  - ${local_branch}